var React = require('react');

var Header = React.createClass({
    render: function () {
        return (
            <header>
                Headerh
            </header>
        )
    }
});

module.exports = Header;